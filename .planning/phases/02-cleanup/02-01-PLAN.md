---
phase: 02-cleanup
plan: 01
type: execute
depends_on: []
files_modified:
  - src/app/api/query/route.ts
  - src/lib/pipeline/adversarial-review.ts
  - src/lib/pipeline/deep-analysis.ts
  - src/lib/pipeline/evidence-gathering.ts
  - src/lib/pipeline/query-understanding.ts
  - src/lib/pipeline/synthesis.ts
  - src/lib/rag.ts
  - src/lib/context-builder.ts
  - src/lib/classification.ts
  - src/app/viewer
  - scripts/extract-full-document.ts
  - scripts/ingest-document.ts
  - scripts/test-extraction.ts
  - package.json
---

<objective>
Delete dead code and audit dependencies. Confirmed dead-code chain via grep:
`src/app/api/query/route.ts` → `src/lib/pipeline/*` (5 files) → `src/lib/rag.ts` + `src/lib/context-builder.ts` + `src/lib/classification.ts`. None of these are imported by anything else.

Purpose: Remove the old multi-stage RAG pipeline that was replaced by the agentic chat (`claude-with-tools.ts`). Remove standalone viewer route, audit one-off scripts, audit deps. Codebase shrinks before Phase 03's project-centric work lands.

Output: Codebase with zero dead modules. `npx tsc --noEmit` still passes. All currently working features still work.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# The dead-code dependency chain (verified by grep on 2026-04-06):
@src/app/api/query/route.ts
@src/lib/rag.ts
@src/lib/pipeline/query-understanding.ts
@src/lib/pipeline/evidence-gathering.ts
@src/lib/pipeline/deep-analysis.ts
@src/lib/pipeline/adversarial-review.ts
@src/lib/pipeline/synthesis.ts
@src/lib/context-builder.ts
@src/lib/classification.ts

# Scripts to evaluate:
@scripts/extract-full-document.ts
@scripts/ingest-document.ts
@scripts/test-extraction.ts

# Verify nothing alive depends on these:
@src/app/api/chat/route.ts
@src/app/api/chat/[id]/route.ts
@src/lib/claude-with-tools.ts

**Tech stack constraint:** Don't downgrade Next.js / Anthropic / OpenAI / Supabase versions. Stable as-is.

**What to NOT touch:**
- Doctrine bodies in DB (just shipped, validated)
- Chat UI / chat-message component
- claude-with-tools.ts
- librarian.ts
- entities.ts
- The hybrid_search RPC
- supabase/migrations/ (no migration changes in this phase)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead RAG pipeline chain</name>
  <files>
    src/app/api/query/route.ts,
    src/lib/pipeline/adversarial-review.ts,
    src/lib/pipeline/deep-analysis.ts,
    src/lib/pipeline/evidence-gathering.ts,
    src/lib/pipeline/query-understanding.ts,
    src/lib/pipeline/synthesis.ts,
    src/lib/rag.ts,
    src/lib/context-builder.ts,
    src/lib/classification.ts,
    src/app/viewer
  </files>
  <action>
Delete the entire dead-code chain in one atomic step. Order doesn't matter because nothing alive imports any of them.

1. Delete `src/app/api/query/route.ts` (the old single-shot query endpoint, replaced by /api/chat).
2. Delete the entire `src/lib/pipeline/` directory recursively (`adversarial-review.ts`, `deep-analysis.ts`, `evidence-gathering.ts`, `query-understanding.ts`, `synthesis.ts`).
3. Delete `src/lib/rag.ts` — the orchestrator for the old pipeline. Imports only from `pipeline/*` and `classification.ts`. Not imported by anything alive.
4. Delete `src/lib/context-builder.ts` — only used by `pipeline/*` and `rag.ts`. Dead.
5. Delete `src/lib/classification.ts` — only used by `pipeline/*` and `rag.ts` and `context-builder.ts`. Dead.
6. Delete the entire `src/app/viewer/` directory recursively if it exists. The new document detail page (`src/app/documents/[id]/page.tsx`) replaces it.

Use `rm -rf` for the directories. Use `rm` for individual files. Do NOT use `git rm` — let `git add -A` handle it in the commit step (the user prefers staging individual files but for deletions a directory at a time is fine).

Why this is safe: I have already grep'd the entire src/ tree. The only references to `pipeline/*` come from `query/route.ts` and `rag.ts`, both being deleted in this same task. The only references to `rag.ts`, `context-builder.ts`, and `classification.ts` come from each other and from the dead pipeline. There is no live import chain into any of this code.

What to avoid: Do NOT touch `src/lib/extraction.ts`, `src/lib/chunking.ts`, `src/lib/embeddings.ts`, `src/lib/search.ts`, `src/lib/doctrine.ts`, `src/lib/intelligence-router.ts`, `src/lib/clients.ts`, `src/lib/web-search.ts`, `src/lib/encryption.ts`, `src/lib/audit.ts`, `src/lib/references.ts`, `src/lib/memory.ts`, `src/lib/entities.ts`, `src/lib/librarian.ts`, `src/lib/claude-with-tools.ts`, `src/lib/normalize.ts`, `src/lib/supabase.ts`, `src/lib/database.types.ts` — these are all alive.
  </action>
  <verify>
After deletion, run `npx tsc --noEmit` and confirm zero TypeScript errors. There should be NO unresolved imports anywhere in the project.

Also run `grep -rn "from.*pipeline" src/ 2>/dev/null | grep -v "\.next"` and confirm zero results. Same for `grep -rn "from.*lib/rag" src/ 2>/dev/null | grep -v "\.next"`, `grep -rn "context-builder" src/ 2>/dev/null | grep -v "\.next"`, `grep -rn "from.*classification" src/ 2>/dev/null | grep -v "\.next"`.
  </verify>
  <done>
- All listed files and directories deleted from disk
- `npx tsc --noEmit` exits with code 0
- Zero grep matches for the dead module imports
- The dev server (if running) still hot-reloads without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit scripts directory and dependencies, remove unused</name>
  <files>
    scripts/extract-full-document.ts,
    scripts/ingest-document.ts,
    scripts/test-extraction.ts,
    package.json
  </files>
  <action>
Two parallel cleanup actions:

**A. Audit `scripts/` directory.** Currently contains three files:
- `scripts/extract-full-document.ts` — likely a one-off extraction test
- `scripts/ingest-document.ts` — likely a CLI ingest helper
- `scripts/test-extraction.ts` — likely a one-off extraction test

For each:
1. `cat` the file to understand purpose
2. Decide: KEEP if it's a long-lived utility that's clearly useful, DELETE if it's a one-shot that already ran
3. Default to DELETE — these are from early experimentation. If a script is keep-worthy, move it to `tools/` (create the dir) and add a one-line comment at the top documenting purpose.

**B. Audit `package.json` dependencies.**
1. Read `package.json`
2. For each dep in `dependencies` and `devDependencies`, run `grep -rn "from \"<package-name>\"" src/ 2>/dev/null | grep -v "\.next" | head -3` to confirm it's actually used
3. Build a list of orphan deps (in package.json but never imported)
4. Remove orphans via `pnpm remove <name>` (one command per orphan, not bulk)
5. EXCEPTIONS — never remove these even if not directly imported:
   - `next`, `react`, `react-dom`, `typescript` (framework)
   - `@types/*` (type-only)
   - `tailwindcss`, `postcss`, `autoprefixer` (build chain)
   - `eslint*`, `prettier*` (tooling)
   - `tsx`, `tsconfig-paths` (script runners)

What to avoid: Do NOT change versions of any dep that stays. Do NOT add new deps. Do NOT touch lockfile manually — let `pnpm remove` handle it.
  </action>
  <verify>
After cleanup:
- `ls scripts/` shows only files explicitly kept (or directory empty/removed)
- `npx tsc --noEmit` still exits with code 0
- `pnpm install` runs cleanly (no missing deps for the remaining code)
- Dev server still starts (`pnpm dev` or check it's already running)
- Run a simple smoke check: `curl -s http://localhost:3004/api/picker?q=test -o /dev/null -w "%{http_code}"` returns 200
  </verify>
  <done>
- `scripts/` audited, every file either deleted or moved to `tools/` with a one-line purpose comment
- `package.json` has no orphan dependencies
- TypeScript still type-checks clean
- Dev server still works
  </done>
</task>

</tasks>

<verification>
Phase 02 Plan 01 complete when:
- [ ] `npx tsc --noEmit` exits with code 0
- [ ] Zero grep matches for `pipeline/`, `lib/rag`, `context-builder`, `from.*classification` in src/
- [ ] `src/app/api/query/`, `src/lib/pipeline/`, `src/lib/rag.ts`, `src/lib/context-builder.ts`, `src/lib/classification.ts`, `src/app/viewer/` all gone
- [ ] `scripts/` cleaned (orphans removed, keepers documented)
- [ ] `package.json` orphan deps removed
- [ ] Dev server still serves homepage (`curl http://localhost:3004` returns 200)
- [ ] Hybrid search still works (`curl http://localhost:3004/api/picker?q=test` returns 200)
</verification>

<success_criteria>
- Both tasks completed
- All verification checks pass
- No TypeScript errors
- No runtime errors in dev server logs
- Codebase is measurably smaller (estimate: ~1500-2000 lines removed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-cleanup/02-01-SUMMARY.md`:

# Phase 02 Plan 01: Dead code removal + dependency audit Summary

**Removed N files (~M lines) of dead RAG pipeline code, replaced by the agentic chat path.**

## Accomplishments
- Deleted dead `query/`, `pipeline/`, `rag.ts`, `context-builder.ts`, `classification.ts`, `viewer/`
- Audited `scripts/` and removed/relocated [details]
- Audited `package.json` and removed [N] orphan dependencies

## Files Created/Modified
- [List actual deletions]

## Decisions Made
- [Per-script keep/delete decisions]
- [Per-dependency removal decisions]

## Issues Encountered
[Or "None"]

## Next Step
Ready for `02-02-PLAN.md` (centralize types + normalize naming)
</output>
