---
phase: 02-cleanup
plan: 01
subsystem: infra
tags: [cleanup, refactor, dead-code, dependencies]

# Dependency graph
requires:
  - phase: 01-chat-rebuild
    provides: [agentic chat path via claude-with-tools.ts that replaced the old multi-stage RAG pipeline]
provides:
  - clean codebase
  - removed dead code (old RAG pipeline chain)
  - removed orphan deps (5 packages)
  - empty scripts/ directory
affects: [02-02, 02-03, 03-projects]

# Tech tracking
tech-stack:
  added: []
  patterns: ["per-task atomic commits", "conservative dep audit (grep verification before remove)"]

key-files:
  created: []
  modified:
    - package.json
    - pnpm-lock.yaml
  deleted:
    - src/app/api/query/route.ts
    - src/lib/pipeline/adversarial-review.ts
    - src/lib/pipeline/deep-analysis.ts
    - src/lib/pipeline/evidence-gathering.ts
    - src/lib/pipeline/query-understanding.ts
    - src/lib/pipeline/synthesis.ts
    - src/lib/rag.ts
    - src/lib/context-builder.ts
    - src/lib/classification.ts
    - scripts/extract-full-document.ts
    - scripts/ingest-document.ts
    - scripts/test-extraction.ts

key-decisions:
  - "Default-delete all three scripts/ files - all were one-shot OCR experiments with hardcoded paths"
  - "Conservative dep removal - keep anything matched even via CSS @import (shadcn)"
  - "Use --allow-empty for Task 1 commit because deleted files were untracked in git"

patterns-established:
  - "Atomic per-task commits with detailed body explaining rationale"
  - "Always grep before pnpm remove - false positives (substring matches like react-pdf inside react-markdown) are real"

issues-created: []

# Metrics
duration: ~10min
completed: 2026-04-06
---

# Phase 02 Plan 01: Dead RAG pipeline removal + dep audit Summary

**Removed 9 dead RAG pipeline files (~1,500 LOC) and 5 orphan dependencies, leaving codebase clean for the project-centric rebuild.**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-04-06
- **Tasks:** 2
- **Files deleted:** 12 (9 src/ + 3 scripts/)
- **Files modified:** 2 (package.json, pnpm-lock.yaml)

## Accomplishments
- Deleted the entire dead multi-stage RAG pipeline (`query/route.ts` -> `pipeline/*` -> `rag.ts` + `context-builder.ts` + `classification.ts`) that was superseded by the agentic chat in phase 01
- Audited `scripts/` and removed all three one-shot OCR experiment scripts
- Audited 19 production deps and removed 5 verified orphans (no src/ imports)
- TypeScript still compiles cleanly; dev server still serves `/` and `/api/picker` with 200

## Task Commits

1. **Task 1: Delete dead RAG pipeline chain** - `a87706f` (refactor)
2. **Task 2: Audit scripts and remove orphan dependencies** - `250718b` (chore)

## Files Created/Modified

### Deleted (src/)
- `src/app/api/query/route.ts` - old single-shot query endpoint
- `src/lib/pipeline/adversarial-review.ts` - dead pipeline stage
- `src/lib/pipeline/deep-analysis.ts` - dead pipeline stage
- `src/lib/pipeline/evidence-gathering.ts` - dead pipeline stage
- `src/lib/pipeline/query-understanding.ts` - dead pipeline stage
- `src/lib/pipeline/synthesis.ts` - dead pipeline stage
- `src/lib/rag.ts` - dead pipeline orchestrator
- `src/lib/context-builder.ts` - dead pipeline helper
- `src/lib/classification.ts` - dead pipeline helper

### Deleted (scripts/)
- `scripts/extract-full-document.ts` - one-shot OCR experiment, hardcoded path
- `scripts/ingest-document.ts` - early standalone CLI ingest, replaced by production pipeline
- `scripts/test-extraction.ts` - one-shot OCR experiment for pages 4-5
- (directory now empty and removed)

### Modified
- `package.json` - removed 5 deps
- `pnpm-lock.yaml` - regenerated by pnpm remove

## Decisions Made

### Per-script (all DELETE)
- **extract-full-document.ts**: hardcoded `/Users/aibrahim/Downloads/document_pdf-5.pdf`, writes to `full_document_output.json`. One-shot. DELETE.
- **ingest-document.ts**: standalone CLI version of the upload pipeline; production lives in `src/app/api/upload` + `librarian.ts` + `extraction.ts`. DELETE.
- **test-extraction.ts**: extracts only pages 4 and 5 from same hardcoded path. One-shot. DELETE.

### Per-dep (REMOVED)
- `@react-pdf-viewer/core` - zero src/ imports; PDF viewer was never wired in
- `react-pdf` - false-matched earlier as substring of `react-markdown`; verified zero direct imports
- `@supabase/ssr` - server code uses `@supabase/supabase-js` directly via `src/lib/supabase.ts`
- `uuid` - zero src/ imports; ids come from Supabase or `crypto`
- `@types/uuid` - companion to uuid; pnpm/npm warned it was a stub anyway

### Per-dep (KEPT after verification)
- `@anthropic-ai/sdk` (claude-with-tools.ts, clients.ts)
- `@base-ui/react` (8 ui/* components)
- `@supabase/supabase-js` (supabase.ts)
- `cohere-ai` (clients.ts)
- `crypto-js` (encryption.ts)
- `next-themes` (sonner.tsx)
- `openai` (clients.ts)
- `pdf-parse` (extraction.ts, librarian.ts, attachments/route.ts, librarian/analyze/route.ts)
- `pdf-to-img` (extraction.ts)
- `pdfjs-dist` (extraction.ts)
- `react-markdown` (chat-message.tsx)
- `remark-gfm` (chat-message.tsx)
- `sonner` (sonner.tsx)
- `tw-animate-css` (globals.css)
- `shadcn` (CSS `@import "shadcn/tailwind.css"` in globals.css — runtime-required)
- All exception-list deps (next, react, tailwind, eslint, typescript, etc.)

## Deviations from Plan

**1. Task 1 commit was empty (--allow-empty)**
- **Issue:** All files in the dead RAG chain were untracked in git (part of a larger uncommitted initial-state set), so deleting them produced no diff.
- **Fix:** Used `git commit --allow-empty` so the per-task atomic-commit milestone is still recorded for the audit trail.
- **Verification:** `git log` shows the commit; the rationale is documented in the commit body.

**2. `src/app/viewer/` did not exist**
- **Issue:** Plan said to delete it "if it exists". It did not.
- **Fix:** Verified with `ls`, noted in commit body.

## Issues Encountered
None.

## Next Phase Readiness
Ready for `02-02-PLAN.md` (centralize types + normalize naming).

---
*Phase: 02-cleanup*
*Completed: 2026-04-06*
