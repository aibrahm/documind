---
phase: 06-deal-room-ui
plan: 01
type: execute
depends_on: ["04-04", "03.5-02"]
files_modified:
  - src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx
  - src/components/negotiation-detail.tsx
---

<objective>
Turn the Negotiations tab from a flat list of cards into an expandable deal-room view. Click a negotiation → it expands inline to show:

- A timeline of events (created, status changes, key_terms updated, linked documents)
- A key facts table with all key_terms fields
- Linked documents (shared via the negotiation_documents M:N table)

No new tables — events are derived from existing data (negotiation row timestamps, project_documents, conversations tagged to the project).

Output: One new component (`NegotiationDetail`) and edits to the Negotiations tab. No backend changes.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-sidebar-workspace/04-04-SUMMARY.md
@src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx
@src/app/api/projects/[id]/conversations/route.ts

**Constraints:**
- No new tables, no schema migrations
- No new API endpoints — derive timeline events from existing data:
  - Negotiation `created_at` → "Negotiation created"
  - Negotiation `updated_at` (when ≠ created_at) → "Last updated"
  - Negotiation `closed_at` → "Closed" event with status
  - Project documents added_at → "Document added"
  - Project conversations created_at → "Conversation started"
- Inline expansion (not a separate route) — keeps URL clean within /projects/[slug]
- Save-as-artifact is Phase 09 — out of scope here
</context>

<tasks>

<task type="auto">
  <name>Task 1: NegotiationDetail expandable component</name>
  <files>src/components/negotiation-detail.tsx</files>
  <action>
Create a client component that takes a negotiation row and a projectId and renders:
- Status badge + counterparty name (if any)
- Key facts table — every field in key_terms as a label/value row, formatted with locale-aware numbers
- Timeline strip — events derived from the negotiation's own dates + the project's documents/conversations, sorted by date desc
- "View linked documents" link that lives at the bottom (links into the Documents tab)

Component fetches from /api/projects/[slug]/documents and /api/projects/[slug]/conversations on mount to compute timeline events. No new endpoints needed.
  </action>
</task>

<task type="auto">
  <name>Task 2: Negotiations tab — click-to-expand</name>
  <files>src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx</files>
  <action>
- Each negotiation card becomes a button that toggles expansion state
- When expanded, render <NegotiationDetail /> below the card header
- Multiple cards can be expanded simultaneously (Set<id> state)
- Add a chevron icon that rotates on expand
- Keep the existing inline key_terms grid as a collapsed-state preview
  </action>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean
- Smoke test: create a project + negotiation with key_terms + link a doc → open the Negotiations tab → click the negotiation → verify expansion shows the timeline and key facts
- Cleanup
</verification>

<success_criteria>
- Negotiations tab is no longer flat — click expands to show the deal-room view
- Timeline events derived from existing data, no new tables needed
- Phase 04 layout untouched
</success_criteria>

<output>
Create .planning/phases/06-deal-room-ui/06-01-SUMMARY.md after completion.
</output>
