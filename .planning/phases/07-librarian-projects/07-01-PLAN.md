---
phase: 07-librarian-projects
plan: 01
type: execute
depends_on: ["03-03", "04-04"]
files_modified:
  - src/lib/librarian.ts
  - src/app/api/librarian/analyze/route.ts
  - src/app/api/upload/route.ts
  - src/app/(workspace)/upload/page.tsx
---

<objective>
Two changes to the librarian flow:

1. **Fix the 31% similarity bug** — sample first/middle/last chunks of each candidate and use the max cosine similarity (instead of just the first chunk).
2. **Add project suggestion** — compute entity overlap between the new document's detected entities and `project_companies`. Surface a project recommendation in the librarian proposal. Render it on the upload page. Wire it through to the upload route so a single confirm both creates the document AND links it to the project.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-librarian-projects/07-CONTEXT.md
@src/lib/librarian.ts
@src/app/api/librarian/analyze/route.ts
@src/app/api/upload/route.ts
@src/app/(workspace)/upload/page.tsx
@.planning/codebase/CONCERNS.md

**Constraints:**
- No schema changes — existing tables (projects, project_companies, project_documents) suffice
- No new dependencies
- The librarian's first-page extraction architecture stays untouched (per STATE.md don't-touch list)
- Project suggestion is additive: if no match, upload behaves exactly as today
- Suggestion only fires when at least one detected entity matches an entity in project_companies
- Max chunk sampling cap: 3 chunks (first, middle, last) — keeps the librarian fast
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 31% bug + add project suggestion to librarian.ts</name>
  <files>src/lib/librarian.ts</files>
  <action>
1. Replace the single-chunk content-similarity sampling in `findRelatedDocuments` with a multi-chunk sampler. Pull up to 3 chunks per candidate (first, middle, last) and take the MAX cosine similarity.
2. Add a new `suggestProject(detectedEntities)` helper that:
   - Canonicalizes the detected entities
   - Looks up `project_companies` rows for any matching entity
   - Joins to `projects` (filtering out archived)
   - Returns the project with the highest entity-overlap count, or null
3. Extend `LibrarianProposal.detected` shape with an optional `suggestedProject` field
4. Call `suggestProject` from `analyzeUpload` after entity detection
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire suggestion through analyze route → upload page → upload route</name>
  <files>src/app/api/librarian/analyze/route.ts, src/app/(workspace)/upload/page.tsx, src/app/api/upload/route.ts</files>
  <action>
1. The analyze route already returns the proposal — it picks up `suggestedProject` automatically
2. Upload page (`(workspace)/upload/page.tsx`) renders a small "Link to project" pill below the existing recommendation card when `suggestedProject` is set, with a toggle (default ON)
3. Upload route accepts a new optional `linkToProject` form field (project UUID); after document creation, inserts a `project_documents` row with `added_by: "librarian"`
4. Upload page passes the toggle state to the upload route in the FormData
  </action>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean
- Smoke test: re-upload the existing Elsewedy memo as a duplicate → librarian should now classify it as `duplicate` (not `related` with 31% similarity)
- Smoke test: with a project linked to "Elsewedy Electric" entity, upload the memo → librarian proposal includes `suggestedProject` matching the project
- Cleanup test data
</verification>

<success_criteria>
- 31% bug fixed
- Project suggestion appears on the upload page when there's an entity overlap
- One-click link works end-to-end
- Old uploads (without project suggestion) still work
</success_criteria>

<output>
Create .planning/phases/07-librarian-projects/07-01-SUMMARY.md after completion.
</output>
