---
phase: 04.5-link-create-ui
plan: 01
type: execute
depends_on: ["04-04"]
files_modified:
  - src/components/link-document-dialog.tsx
  - src/components/create-negotiation-dialog.tsx
  - src/app/(workspace)/projects/[slug]/_tabs/documents.tsx
  - src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx
---

<objective>
Add the two missing UI affordances that make the Phase 04 workspace tabs actually usable: a "+ Link document" button on the Documents tab and a "+ New negotiation" button on the Negotiations tab. After this plan, the user can do everything from the workspace UI with no curl required.

Output: Two new dialog components + edits to two tab components. Both flows wired to the Phase 03 REST endpoints (no new APIs needed).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-sidebar-workspace/04-04-SUMMARY.md
@src/app/(workspace)/projects/[slug]/_tabs/documents.tsx
@src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx
@src/components/create-project-dialog.tsx
@src/components/ui/dialog.tsx

# Existing API endpoints these dialogs use:
@src/app/api/projects/[id]/documents/route.ts
@src/app/api/negotiations/route.ts
@src/app/api/picker/route.ts

**Constraints:**
- No new server actions needed — fetch directly from the dialogs (small surface, no revalidation needed since tabs re-fetch on the next mount)
- Match the existing CreateProjectDialog patterns: shadcn Dialog, useTransition for pending state, fail-loud error surfacing
- Documents tab: link via the picker so the user can search any current document
- Negotiations tab: minimal form (name + optional counterparty entity_id)
- Both dialogs trigger a tab refresh by lifting a `refreshKey` state into the tab and incrementing it on success
- Counterparty picker reuses `/api/picker?q=` and filters to `companies`
</context>

<tasks>

<task type="auto">
  <name>Task 1: LinkDocumentDialog + Documents tab wiring</name>
  <files>src/components/link-document-dialog.tsx, src/app/(workspace)/projects/[slug]/_tabs/documents.tsx</files>
  <action>
Create a new dialog component that searches documents via /api/picker and POSTs to /api/projects/[slug]/documents on confirm. Add a "+ Link document" button to the Documents tab that opens it. Tab refreshes on success.
  </action>
</task>

<task type="auto">
  <name>Task 2: CreateNegotiationDialog + Negotiations tab wiring</name>
  <files>src/components/create-negotiation-dialog.tsx, src/app/(workspace)/projects/[slug]/_tabs/negotiations.tsx</files>
  <action>
Create a new dialog with name (required) + counterparty picker (optional) + status (defaults to "open"). POSTs to /api/negotiations with the projectId. Add a "+ New negotiation" button to the Negotiations tab.
  </action>
</task>

</tasks>

<verification>
- npx tsc --noEmit clean
- Smoke test: open a workspace, click both buttons, link a real doc, create a real negotiation, verify both appear in the respective tabs after the dialogs close
- Cleanup test artifacts
</verification>

<success_criteria>
The workspace is fully usable from the UI — no more curl required for daily flows.
</success_criteria>

<output>
Create .planning/phases/04.5-link-create-ui/04.5-01-SUMMARY.md after completion.
</output>
