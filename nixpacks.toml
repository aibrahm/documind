[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["echo 'no build needed — MCP server runs via tsx'"]

[start]
cmd = "npx tsx src/mcp-server.ts"
